class Signos {
  
  //PROPIEDADES-----------------------------------------------------------------------------------------------------------------------------------------------------------------

  float x, y;
  int tam;
  float vel;
  PImage signos;
  
  //CONSTRUCTOR-----------------------------------------------------------------------------------------------------------------------------------------------------------------

  Signos() {
    
    x = random (0, width);
    y = random(-height, 0);

    tam = 100;
    vel = random(1, 3);

    signos = loadImage(int (random(9)) + ".png");
    signos.resize(int (tam), int (tam));
  }

  //MÉTODOS-----------------------------------------------------------------------------------------------------------------------------------------------------------------

  void dibujar() {

    image(signos, x, y);
 
  }
  
  void actualizar() {
    y+= vel;

  }
  
}
