//MI CLASE FONDO

//PROPIEDADES

//color (para piso, usando un rect), color (para cielo, usando un rect), tamanio (de los arboles), PImage (arboles), PImage (para los copos de nieve)

//CONSTRUCTOR

//por parámetro, definir el tamaño de los árboles

//la posición y el tamaño del cielo es==> rect(0,0,600,250);
// color del cielo ==> color(#640000); 

//la posicion y el tamaño del piso es==> rect(0,250,600,250);
//color del piso==> color(#B2B3FF);

//la posición de los árboles es fija

//Se cargan las imágenes

//MÉTODOS

//dibujarFondo(), dibujarArboles(), nevar()

//dibujarFondo()==> Dibuja los rectángulos de cielo y de piso con las posiciones fijadas en el constructor. 
//dibujarArboles()==> Dibuja los arboles usando un for() en las posiciones fijadas por la clase y del tamaño elegido en los parámetros del constructor
//nevar()==> Dibuja los copos de nieve en posciones random, usando un for(), y se ejecuta con el mousePressed()
